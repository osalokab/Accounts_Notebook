# خطة تنفيذ تطبيق دفتر الحسابات باستخدام Flutter

## 1. الهدف العام

بناء تطبيق Flutter حديث يحاكي طريقة عمل تطبيق **دفتر الحسابات** اعتماداً
على الواجهات والوظائف المستخلصة من الملف المرجعي، مع الحفاظ على منطق
التطبيق الأساسي، وإعادة تنفيذ الواجهات بأسلوب حديث ومتجاوب.

يجب أن يكون التطبيق:

-   باللغة العربية بشكل أساسي.
-   داعماً لاتجاه RTL.
-   مبنياً بالكامل باستخدام Flutter.
-   قابلاً للتشغيل والمعاينة على جهاز حقيقي أو Emulator قبل إنشاء APK.
-   منظماً على شكل مشروع قابل للصيانة والتطوير.
-   معتمداً على قاعدة بيانات محلية للبيانات الأساسية.
-   قابلاً لإضافة الوظائف المتقدمة لاحقاً دون إعادة بناء المشروع من الصفر.
-   محافظاً على الفصل بين واجهة المستخدم والمنطق وقاعدة البيانات
    والخدمات.

> **قاعدة أساسية:** لا يتم البدء بإضافة وظائف غير موجودة في نطاق التطبيق
> المرجعي إلا إذا كانت ضرورية تقنياً لتنفيذ وظيفة موجودة أو تم طلبها
> صراحة.

------------------------------------------------------------------------

# 2. نطاق التطبيق

يجب أن يغطي التطبيق الوحدات التالية:

1.  شاشة الترحيب.
2.  الشاشة الرئيسية.
3.  القائمة الجانبية.
4.  الحسابات.
5.  العملاء.
6.  الموردون.
7.  كشف الحساب.
8.  إضافة مبلغ.
9.  تعديل عملية.
10. حذف عملية.
11. تسوية/إغلاق الحساب.
12. البحث.
13. الفرز والترتيب.
14. العملات.
15. التصنيفات.
16. التقارير.
17. التقرير الإجمالي.
18. تقرير تفاصيل المبالغ.
19. التقرير الشهري.
20. حركة الحسابات.
21. العمليات المتكررة.
22. النسخ الاحتياطي المحلي.
23. الاستعادة.
24. Google Drive كمرحلة متقدمة.
25. مشاركة البيانات.
26. إنشاء PDF.
27. إعدادات البيانات الشخصية.
28. إعدادات الطباعة.
29. إعدادات الأمان.
30. إعدادات العملات.
31. إعدادات التصنيفات.
32. التنبيهات.
33. الدعم.
34. حول التطبيق.
35. مشاركة التطبيق.
36. الخروج/إغلاق التطبيق حسب المنصة.

------------------------------------------------------------------------

# 3. مبدأ التنفيذ

يجب تنفيذ المشروع على مراحل متدرجة، وعدم بناء جميع الوظائف دفعة واحدة.

الترتيب الإجباري:

``` text
تحليل الواجهات
    ↓
تصميم بنية المشروع
    ↓
إنشاء Theme وRTL
    ↓
بناء الواجهات الأساسية
    ↓
بناء Models
    ↓
بناء قاعدة البيانات
    ↓
ربط الحسابات
    ↓
ربط العمليات
    ↓
البحث والفرز
    ↓
التقارير
    ↓
النسخ والاستعادة
    ↓
PDF والمشاركة
    ↓
الأمان والتنبيهات
    ↓
الوظائف المتقدمة
    ↓
الاختبار
    ↓
المعاينة على الجهاز
    ↓
إخراج APK
```

------------------------------------------------------------------------

# 4. المرحلة الأولى: تحليل المرجع

قبل كتابة أي كود يجب تحليل كل واجهة موجودة في المرجع.

لكل شاشة يجب تسجيل:

-   اسم الشاشة.
-   عنوان الشاشة.
-   العناصر الموجودة.
-   أماكن العناصر.
-   الأزرار.
-   الأيقونات.
-   الحقول.
-   الألوان.
-   أحجام النصوص.
-   طريقة التنقل.
-   الإجراءات التي تحدث عند الضغط.
-   رسائل التأكيد.
-   رسائل الخطأ.
-   الحالات الفارغة.
-   حالات التحميل.
-   حالات عدم وجود بيانات.

يجب عدم افتراض وظيفة لعنصر لا يدعمها المرجع.

------------------------------------------------------------------------

# 5. خريطة الشاشات

## 5.1 شاشة الترحيب

المحتوى:

-   شعار/عنوان التطبيق.
-   وصف مختصر.
-   معلومات تعريفية.
-   زر الإغلاق أو المتابعة حسب التصميم النهائي.

الهدف:

إظهار شاشة البداية بطريقة مشابهة للمرجع.

------------------------------------------------------------------------

## 5.2 الشاشة الرئيسية

تحتوي على:

-   AppBar.
-   اسم التطبيق.
-   زر القائمة الجانبية.
-   التنبيهات عند الحاجة.
-   عرض الحسابات.
-   عرض العملة الحالية.
-   الرصيد/الإجمالي.
-   زر إضافة عملية.
-   البحث.
-   الانتقال بين العملات أو الأقسام.

------------------------------------------------------------------------

## 5.3 القائمة الجانبية

يجب أن تحتوي على:

``` text
إضافة مبلغ
التقارير
  ├── إجمالي المبالغ
  ├── تفاصيل المبالغ
  ├── الإجمالي الشهري
  └── حركة الحسابات

التكرار التلقائي

النسخ الاحتياطي
  ├── استرجاع قاعدة البيانات
  └── Google Drive

الإعدادات
الدعم
حول التطبيق
مشاركة التطبيق
الخروج
```

يجب أن تكون القائمة قابلة للتمرير إذا زاد عدد العناصر.

------------------------------------------------------------------------

# 6. الحسابات

## 6.1 شاشة الحسابات

تحتوي على:

-   شريط بحث.
-   قائمة الحسابات.
-   اسم الحساب.
-   التصنيف.
-   العملة.
-   الرصيد.
-   إمكانية فتح كشف الحساب.
-   إضافة حساب.
-   تعديل الحساب.
-   حذف الحساب عند السماح بذلك.

## 6.2 أنواع الحسابات

يجب دعم التصنيفات الأساسية الظاهرة في المرجع، مثل:

-   عام.
-   عملاء.
-   موردون.

مع إمكانية توسيع التصنيفات من الإعدادات.

------------------------------------------------------------------------

# 7. كشف الحساب

عند فتح حساب معين:

``` text
اسم الحساب
العملة
الرصيد الحالي
--------------------
العمليات
--------------------
التاريخ
نوع العملية
المبلغ
الوصف
الرصيد بعد العملية
```

الوظائف:

-   إضافة عملية.
-   تعديل عملية.
-   حذف عملية.
-   البحث داخل العمليات.
-   ترتيب العمليات.
-   عرض التفاصيل.
-   مشاركة كشف الحساب.
-   تصدير PDF.
-   تسوية الحساب إذا كانت متاحة.

------------------------------------------------------------------------

# 8. إضافة مبلغ

يجب أن تحتوي الشاشة على:

-   اختيار الحساب.
-   البحث عن الحساب.
-   التاريخ.
-   العملة.
-   نوع العملية.
-   المبلغ.
-   الوصف.
-   زر الحفظ.

## التحقق

لا يسمح بالحفظ إذا:

-   لم يتم اختيار الحساب.
-   المبلغ غير صالح.
-   المبلغ أقل من أو يساوي الصفر إذا كان النظام يتطلب ذلك.
-   العملة غير محددة.
-   التاريخ غير صالح.

رسائل الخطأ يجب أن تكون عربية وواضحة.

------------------------------------------------------------------------

# 9. منطق العمليات المالية

يجب تعريف نوعي العملية بوضوح:

``` text
CREDIT
DEBIT
```

ويجب توحيد طريقة حساب الرصيد في جميع أجزاء التطبيق.

مثال منطقي:

``` text
الرصيد الجديد = الرصيد السابق + قيمة العملية
```

أو

``` text
الرصيد الجديد = الرصيد السابق - قيمة العملية
```

بحسب نوع العملية كما هو محدد في منطق التطبيق.

يجب عدم تكرار منطق الحساب في أكثر من شاشة.

يجب وضعه في طبقة Logic/Repository/Service واحدة.

------------------------------------------------------------------------

# 10. البحث

يجب توفير البحث في:

-   الحسابات.
-   العملاء.
-   الموردين.
-   العمليات.
-   التقارير عند الحاجة.

البحث يجب أن:

-   يعمل أثناء الكتابة.
-   يتجاهل المسافات الزائدة.
-   يدعم العربية.
-   لا يؤدي إلى تجميد الواجهة.
-   يعرض حالة عدم وجود نتائج.

------------------------------------------------------------------------

# 11. الفرز والترتيب

يجب دعم الترتيب حسب:

-   الاسم.
-   التاريخ.
-   المبلغ.
-   الرصيد.

مع:

``` text
تصاعدي
تنازلي
```

ويجب أن يكون ترتيب البيانات مستقلاً عن البحث.

------------------------------------------------------------------------

# 12. العملات

إنشاء وحدة مستقلة للعملات.

كل عملة تحتوي على:

``` text
id
name
symbol
isDefault
createdAt
```

يجب دعم:

-   عرض العملة.
-   إضافة عملة.
-   تعديل عملة.
-   حذف عملة إذا لم تكن مرتبطة ببيانات تمنع الحذف.
-   تحديد العملة الافتراضية.

------------------------------------------------------------------------

# 13. التصنيفات

Model:

``` text
id
name
type
createdAt
```

الوظائف:

-   إضافة.
-   تعديل.
-   حذف.
-   اختيار التصنيف أثناء إنشاء الحساب.
-   تصفية الحسابات حسب التصنيف.

------------------------------------------------------------------------

# 14. التقارير

## 14.1 إجمالي المبالغ

يعرض:

-   إجمالي المدين.
-   إجمالي الدائن.
-   الرصيد.
-   عدد الحسابات.
-   عدد العمليات.

------------------------------------------------------------------------

## 14.2 تقرير تفاصيل المبالغ

يعرض:

-   التاريخ.
-   الحساب.
-   نوع العملية.
-   المبلغ.
-   العملة.
-   الوصف.

الوظائف:

-   فلترة.
-   بحث.
-   ترتيب.
-   مشاركة.
-   PDF.

------------------------------------------------------------------------

## 14.3 التقرير الشهري

يعرض البيانات بحسب الشهر:

``` text
يناير
فبراير
مارس
...
```

ويجب حساب الإجمالي من قاعدة البيانات وليس تخزين أرقام ثابتة.

يمكن لاحقاً إضافة رسم بياني إذا كان متوافقاً مع تصميم المشروع.

------------------------------------------------------------------------

## 14.4 حركة الحسابات

تعرض:

-   الحساب.
-   عدد العمليات.
-   إجمالي المدين.
-   إجمالي الدائن.
-   الرصيد.
-   الفترة الزمنية.

------------------------------------------------------------------------

# 15. العمليات المتكررة

إنشاء Model:

``` text
id
accountId
amount
type
description
frequency
startDate
nextExecutionDate
isActive
```

الفترات:

-   يومي.
-   أسبوعي.
-   شهري.

يجب عدم إنشاء عمليات فعلية بشكل عشوائي.

يتم إنشاء العملية عند حلول موعدها وفق نظام واضح.

------------------------------------------------------------------------

# 16. قاعدة البيانات

يجب استخدام قاعدة بيانات محلية مناسبة لتطبيق Flutter.

الحد الأدنى من الجداول/Collections:

``` text
accounts
transactions
currencies
categories
recurring_transactions
settings
backup_metadata
```

العلاقات:

``` text
Account
   │
   ├── Currency
   ├── Category
   └── Transactions

RecurringTransaction
   └── Account
```

------------------------------------------------------------------------

# 17. Models

إنشاء Models مستقلة:

``` text
Account
Transaction
Currency
Category
RecurringTransaction
AppSettings
PersonalData
PrintSettings
```

كل Model يجب أن يدعم:

-   Constructor.
-   Serialization.
-   Deserialization.
-   Copy/update عند الحاجة.
-   تحويل آمن للبيانات.

------------------------------------------------------------------------

# 18. بنية المشروع

يجب عدم وضع كل الكود داخل `main.dart`.

البنية المقترحة:

``` text
lib/
│
├── main.dart
│
├── app/
│   ├── app.dart
│   ├── routes.dart
│   └── theme.dart
│
├── models/
│   ├── account.dart
│   ├── transaction.dart
│   ├── currency.dart
│   ├── category.dart
│   ├── recurring_transaction.dart
│   └── app_settings.dart
│
├── screens/
│   ├── welcome/
│   ├── home/
│   ├── accounts/
│   ├── transactions/
│   ├── reports/
│   ├── recurring/
│   ├── backup/
│   ├── settings/
│   └── about/
│
├── widgets/
│   ├── account_card.dart
│   ├── transaction_card.dart
│   ├── balance_card.dart
│   ├── search_bar.dart
│   ├── app_drawer.dart
│   └── app_button.dart
│
├── services/
│   ├── database_service.dart
│   ├── backup_service.dart
│   ├── pdf_service.dart
│   ├── sharing_service.dart
│   └── notification_service.dart
│
├── repositories/
│   ├── account_repository.dart
│   ├── transaction_repository.dart
│   ├── currency_repository.dart
│   └── category_repository.dart
│
└── utils/
    ├── validators.dart
    ├── formatters.dart
    └── constants.dart
```

------------------------------------------------------------------------

# 19. إدارة الحالة

يجب استخدام نظام واضح لإدارة الحالة.

يمكن اختيار أحد الحلول المناسبة مثل:

-   Provider.
-   Riverpod.
-   Bloc/Cubit.

المهم هو:

-   عدم وضع Business Logic داخل Widgets.
-   عدم تنفيذ استعلامات قاعدة البيانات مباشرة داخل الواجهات.
-   تحديث الواجهة تلقائياً عند تغير البيانات.
-   فصل State عن UI.

------------------------------------------------------------------------

# 20. Theme

إنشاء Theme موحد.

المواصفات:

``` text
Direction: RTL
Locale: ar
Material Design: Material 3
```

يجب تحديد:

-   Primary Color.
-   Secondary Color.
-   Background.
-   Card Theme.
-   Input Theme.
-   Button Theme.
-   AppBar Theme.
-   Dialog Theme.
-   Typography.
-   Border Radius.
-   Spacing.

يجب عدم تكرار الألوان والأحجام داخل الملفات.

------------------------------------------------------------------------

# 21. دعم اللغة العربية

يجب أن يكون:

``` text
Text Direction = RTL
```

مع مراعاة:

-   محاذاة النصوص.
-   اتجاه الأيقونات.
-   الأسهم.
-   حقول الأرقام.
-   التواريخ.
-   أسماء الأشهر العربية عند الحاجة.

الأرقام المالية يجب تنسيقها بشكل موحد.

------------------------------------------------------------------------

# 22. التصميم Responsive

التطبيق يجب أن يعمل على:

-   هاتف Android.
-   شاشات مختلفة الأحجام.
-   Emulator.
-   Tablet عند الحاجة.

يجب عدم استخدام:

``` text
Hard-coded width
Hard-coded height
```

بشكل يسبب انهيار الواجهة.

استخدام:

-   `Expanded`
-   `Flexible`
-   `LayoutBuilder`
-   `MediaQuery`
-   `SafeArea`

حسب الحاجة.

------------------------------------------------------------------------

# 23. المشاركة

يجب توفير طبقة `SharingService`.

تدعم:

``` text
Text
PDF
Image
```

ولا يجب أن تكون عملية المشاركة مرتبطة مباشرة بواجهة معينة.

------------------------------------------------------------------------

# 24. PDF

يجب إنشاء PDF يحتوي على:

-   بيانات المستخدم.
-   اسم الحساب.
-   العملة.
-   الفترة.
-   العمليات.
-   الرصيد.
-   التاريخ.

ويجب أن يكون مناسباً للغة العربية قدر الإمكان.

------------------------------------------------------------------------

# 25. النسخ الاحتياطي والاستعادة

## النسخ المحلي

يجب توفير:

``` text
Export Backup
Import Backup
```

ويجب:

1.  إنشاء نسخة من البيانات.
2.  التحقق من سلامة الملف.
3.  حفظ البيانات.
4.  استعادة البيانات.
5.  التحقق من توافق الإصدار.
6.  عرض رسالة نجاح/فشل.

يجب عدم حذف قاعدة البيانات الحالية قبل التأكد من أن النسخة المستوردة
صالحة.

------------------------------------------------------------------------

# 26. Google Drive

يتم تنفيذها كمرحلة متقدمة بعد استقرار النسخ المحلي.

المراحل:

``` text
تسجيل الدخول
    ↓
طلب الصلاحيات
    ↓
رفع النسخة
    ↓
حفظ بيانات آخر Backup
    ↓
استعادة النسخة
```

لا يجب جعل Google Drive شرطاً لتشغيل التطبيق الأساسي.

------------------------------------------------------------------------

# 27. الأمان

يجب توفير:

-   قفل التطبيق.
-   كلمة مرور.
-   تخزين آمن للبيانات الحساسة.
-   عدم حفظ كلمة المرور كنص واضح.
-   قفل تلقائي عند الحاجة.

يجب أن يكون نظام الأمان قابلاً للتفعيل والتعطيل من الإعدادات.

------------------------------------------------------------------------

# 28. الإعدادات

تنقسم إلى:

``` text
الإعدادات
│
├── البيانات الشخصية
├── خيارات الطباعة
├── الأمان
├── العملات
├── التصنيفات
├── النسخ الاحتياطي
├── الإشعارات
└── معلومات التطبيق
```

------------------------------------------------------------------------

# 29. البيانات الشخصية

الحقول المقترحة:

``` text
اسم المستخدم
اسم المؤسسة
رقم الهاتف
العنوان
البريد الإلكتروني
الشعار
```

تستخدم هذه البيانات عند إنشاء التقارير وملفات PDF.

------------------------------------------------------------------------

# 30. الإشعارات

يجب دعم:

-   إشعارات العمليات المتكررة.
-   إشعارات النسخ الاحتياطي.
-   التنبيهات المهمة.

ويجب عدم طلب صلاحيات الإشعارات إلا عند الحاجة.

------------------------------------------------------------------------

# 31. حالات الواجهات

كل شاشة تحتوي على الحالات التالية عند الحاجة:

### Loading

``` text
جارٍ التحميل...
```

### Empty

``` text
لا توجد بيانات
```

### Error

``` text
حدث خطأ
[إعادة المحاولة]
```

### Success

``` text
تمت العملية بنجاح
```

يجب عدم ترك الشاشة فارغة أثناء تنفيذ العمليات.

------------------------------------------------------------------------

# 32. التعامل مع الأخطاء

يجب إنشاء طبقة موحدة للتعامل مع الأخطاء.

الأخطاء المحتملة:

-   فشل قاعدة البيانات.
-   فشل حفظ العملية.
-   فشل استعادة النسخة.
-   ملف Backup غير صالح.
-   فشل إنشاء PDF.
-   فشل المشاركة.
-   رفض صلاحية.
-   فشل الاتصال بالخدمة الخارجية.

كل خطأ يجب أن ينتج رسالة مفهومة للمستخدم.

------------------------------------------------------------------------

# 33. قواعد مهمة للبيانات المالية

هذه النقطة إلزامية.

يجب:

-   عدم استخدام `double` للعمليات المالية إذا كان ذلك يؤدي إلى أخطاء
    دقة.
-   استخدام طريقة آمنة لتخزين القيم المالية.
-   توحيد التقريب.
-   منع القيم غير الصالحة.
-   عدم تغيير عملية محفوظة دون تحديث الرصيد بطريقة صحيحة.
-   عند حذف عملية يجب إعادة حساب الرصيد.
-   عند تعديل عملية يجب عكس تأثيرها القديم ثم تطبيق التأثير الجديد.
-   عند تغيير العملة يجب عدم خلط أرصدة العملات المختلفة.

------------------------------------------------------------------------

# 34. الاختبارات

يجب إنشاء اختبارات على الأقل للمنطق الأساسي.

## Unit Tests

اختبار:

-   حساب الرصيد.
-   إضافة عملية.
-   تعديل عملية.
-   حذف عملية.
-   الفرز.
-   البحث.
-   التقارير.
-   العمليات المتكررة.

## Widget Tests

اختبار:

-   شاشة إضافة مبلغ.
-   شاشة الحسابات.
-   كشف الحساب.
-   القائمة الجانبية.

## Integration Tests

اختبار السيناريو الكامل:

``` text
إنشاء حساب
    ↓
إضافة عملية
    ↓
ظهور الرصيد
    ↓
تعديل العملية
    ↓
تحديث الرصيد
    ↓
حذف العملية
    ↓
عودة الرصيد
```

------------------------------------------------------------------------

# 35. اختبار سيناريوهات المستخدم

يجب اختبار:

### السيناريو 1

``` text
فتح التطبيق
→ الشاشة الرئيسية
→ إضافة حساب
→ إضافة مبلغ
→ ظهور الرصيد
```

### السيناريو 2

``` text
فتح حساب
→ عرض العمليات
→ تعديل عملية
→ التأكد من الرصيد
```

### السيناريو 3

``` text
فتح حساب
→ حذف عملية
→ التأكد من إعادة حساب الرصيد
```

### السيناريو 4

``` text
إضافة عدة حسابات
→ البحث
→ الفرز
→ فتح الحساب الصحيح
```

### السيناريو 5

``` text
إنشاء Backup
→ حذف/تغيير بيانات تجريبية
→ Restore
→ التأكد من عودة البيانات
```

------------------------------------------------------------------------

# 36. البيانات التجريبية

أثناء التطوير يجب إنشاء Seed Data اختيارية، مثل:

``` text
أحمد محمد
محمد علي
خالد حسن
شركة تجريبية
```

مع عمليات تجريبية مختلفة.

يجب أن تكون هذه البيانات منفصلة عن بيانات المستخدم الحقيقية.

------------------------------------------------------------------------

# 37. المعاينة أثناء التطوير

يجب أن تكون الأولوية:

``` text
Flutter Run
    ↓
جهاز حقيقي / Emulator
    ↓
Hot Reload
    ↓
تعديل الواجهة
    ↓
اختبار
```

قبل إنشاء APK.

لا يتم اعتبار التطبيق جاهزاً للإصدار لمجرد أن المشروع يقوم بالـ Build.

يجب تشغيله فعلياً واختبار الواجهات.

------------------------------------------------------------------------

# 38. استراتيجية التنفيذ العملية

## Sprint 1 --- إنشاء المشروع

المهام:

-   إنشاء Flutter Project.
-   ضبط Android.
-   ضبط اللغة العربية.
-   ضبط RTL.
-   إنشاء Theme.
-   إنشاء Routing.
-   إنشاء بنية المجلدات.

الناتج:

تطبيق فارغ منظم وقابل للتشغيل.

------------------------------------------------------------------------

## Sprint 2 --- الواجهات الأساسية

تنفيذ:

-   Welcome.
-   Home.
-   Drawer.
-   Accounts.
-   Account Details.
-   Add Transaction.

الناتج:

نسخة UI قابلة للتنقل.

------------------------------------------------------------------------

## Sprint 3 --- قاعدة البيانات

تنفيذ:

-   Database.
-   Models.
-   Repositories.
-   CRUD.

الناتج:

بيانات حقيقية محلية.

------------------------------------------------------------------------

## Sprint 4 --- العمليات المالية

تنفيذ:

-   إضافة.
-   تعديل.
-   حذف.
-   حساب الرصيد.
-   كشف الحساب.
-   البحث.
-   الفرز.

الناتج:

الجزء الأساسي من دفتر الحسابات يعمل فعلياً.

------------------------------------------------------------------------

## Sprint 5 --- العملات والتصنيفات

تنفيذ:

-   العملات.
-   التصنيفات.
-   ربطها بالحسابات.
-   فلترة الحسابات.

------------------------------------------------------------------------

## Sprint 6 --- التقارير

تنفيذ:

-   إجمالي المبالغ.
-   تفاصيل العمليات.
-   التقرير الشهري.
-   حركة الحسابات.

------------------------------------------------------------------------

## Sprint 7 --- المشاركة وPDF

تنفيذ:

-   إنشاء PDF.
-   مشاركة PDF.
-   مشاركة نص.
-   مشاركة صورة عند الحاجة.

------------------------------------------------------------------------

## Sprint 8 --- Backup

تنفيذ:

-   Export.
-   Import.
-   Validation.
-   Restore.
-   حماية البيانات الحالية.

------------------------------------------------------------------------

## Sprint 9 --- الوظائف المتقدمة

تنفيذ:

-   العمليات المتكررة.
-   الإشعارات.
-   الأمان.
-   Google Drive.
-   الوصول من الكمبيوتر إذا كان ضمن نطاق النسخة النهائية.

------------------------------------------------------------------------

## Sprint 10 --- الاختبار والتحسين

تنفيذ:

-   إصلاح الأخطاء.
-   تحسين الأداء.
-   تحسين Responsive UI.
-   اختبار RTL.
-   اختبار البيانات المالية.
-   اختبار Backup/Restore.
-   اختبار PDF.
-   اختبار المشاركة.

------------------------------------------------------------------------

# 39. معايير قبول التطبيق

لا تعتبر المرحلة مكتملة إلا إذا:

-   التطبيق يعمل بدون Crash في السيناريوهات الأساسية.
-   يمكن إنشاء حساب.
-   يمكن إضافة عملية.
-   يمكن تعديل العملية.
-   يمكن حذف العملية.
-   الرصيد يحسب بشكل صحيح.
-   البحث يعمل.
-   الفرز يعمل.
-   كشف الحساب يعمل.
-   العملات تعمل.
-   التصنيفات تعمل.
-   التقارير تعرض بيانات فعلية.
-   Backup يعمل.
-   Restore يعمل.
-   PDF يعمل.
-   المشاركة تعمل.
-   RTL يعمل.
-   الواجهات متجاوبة.
-   البيانات لا تضيع بعد إغلاق التطبيق وإعادة فتحه.

------------------------------------------------------------------------

# 40. ترتيب الأولويات

## أولوية P0 --- ضروري

``` text
UI الأساسي
الحسابات
العمليات
الرصيد
قاعدة البيانات
البحث
الفرز
كشف الحساب
```

## أولوية P1 --- مهم

``` text
العملات
التصنيفات
التقارير
PDF
المشاركة
Backup/Restore
```

## أولوية P2 --- متقدم

``` text
العمليات المتكررة
الإشعارات
الأمان
Google Drive
الوصول من الكمبيوتر
```

------------------------------------------------------------------------

# 41. قواعد عدم الانحراف عن المشروع

يجب الالتزام بالآتي:

1.  عدم تغيير فكرة التطبيق.
2.  عدم إضافة نظام محاسبي معقد غير موجود في النطاق.
3.  عدم إضافة تسجيل دخول سحابي كشرط للتطبيق.
4.  عدم الاعتماد على الإنترنت للوظائف الأساسية.
5.  عدم حذف أي وظيفة رئيسية من المرجع.
6.  عدم وضع كل الكود في ملف واحد.
7.  عدم ربط UI مباشرة بقاعدة البيانات.
8.  عدم وضع Business Logic داخل Widgets.
9.  عدم استخدام بيانات ثابتة بدلاً من قاعدة البيانات في النسخة النهائية.
10. عدم اعتبار التطبيق مكتملاً قبل اختباره على جهاز فعلي أو Emulator.

------------------------------------------------------------------------

# 42. Definition of Done

يعتبر المشروع مكتملاً عندما يتحقق الآتي:

``` text
[✓] Flutter Project
[✓] RTL
[✓] Theme
[✓] Navigation
[✓] Home
[✓] Drawer
[✓] Accounts
[✓] Transactions
[✓] Balance
[✓] Search
[✓] Sorting
[✓] Currencies
[✓] Categories
[✓] Reports
[✓] Recurring Transactions
[✓] Backup
[✓] Restore
[✓] PDF
[✓] Sharing
[✓] Security
[✓] Notifications
[✓] Error Handling
[✓] Unit Tests
[✓] Widget Tests
[✓] Integration Tests
[✓] Device Testing
[✓] Release Build
[✓] APK
```

------------------------------------------------------------------------

# 43. النتيجة النهائية المستهدفة

النتيجة النهائية يجب أن تكون تطبيق Flutter عربي متكامل يحاكي **دفتر
الحسابات** من حيث:

``` text
الواجهات
+
التنقل
+
إدارة الحسابات
+
إدارة العمليات
+
حساب الأرصدة
+
العملات
+
التصنيفات
+
التقارير
+
النسخ الاحتياطي
+
المشاركة
+
PDF
+
الإعدادات
+
الأمان
```

مع بنية برمجية نظيفة تسمح بتطوير التطبيق لاحقاً دون إعادة بناء المشروع
بالكامل.

------------------------------------------------------------------------

# 44. ترتيب التنفيذ النهائي المختصر

``` text
01. تحليل المرجع
02. إنشاء Flutter Project
03. إعداد RTL وTheme
04. إنشاء Routing
05. بناء Welcome
06. بناء Home
07. بناء Drawer
08. بناء Accounts UI
09. بناء Account Details
10. بناء Add Transaction
11. إنشاء Models
12. إنشاء Database
13. إنشاء Repositories
14. ربط الحسابات
15. ربط العمليات
16. حساب الأرصدة
17. البحث والفرز
18. العملات
19. التصنيفات
20. التقارير
21. العمليات المتكررة
22. PDF
23. المشاركة
24. Backup/Restore
25. الأمان
26. الإشعارات
27. Google Drive
28. الاختبارات
29. اختبار الجهاز
30. تحسين UI/UX
31. Build Release
32. إنشاء APK
```

## قاعدة العمل النهائية

**لا تنتقل إلى المرحلة التالية قبل التأكد من أن المرحلة الحالية تعمل
بشكل صحيح.**

والأولوية دائماً:

``` text
Correctness
    ↓
Data Safety
    ↓
Functionality
    ↓
UI Accuracy
    ↓
Performance
    ↓
Visual Polish
```

بهذا الترتيب يتم الحصول على تطبيق قابل للاستخدام فعلياً، وليس مجرد نسخة
شكلية من الواجهات.
